// Alias route for naming consistency.
// Prefer using /api/profiles/[id]/images going forward; /api/profile-detail/[id]/images remains supported.

export { GET } from "../../../profile-detail/[id]/images/route";
