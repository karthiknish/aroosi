# Prevent npm from installing optional dependencies
omit=optional

# Use exact versions to prevent unexpected updates
save-exact=true

# Ensure npm doesn't install workspace packages that aren't referenced
workspaces-update=false